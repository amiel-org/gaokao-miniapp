# 高考志愿填报小程序项目结构说明

## 1. 目标

这个项目不是单纯的小程序页面工程，而是一个包含前端、小型后端、数据整理和定时更新能力的完整项目。

所以目录结构必须从一开始就把职责分开，避免后期混乱。

当前结构设计目标：

- 让前端、小程序代码独立清晰
- 让数据脚本和业务规则独立可维护
- 让后续自动更新流程有明确落点
- 让文档、数据、日志、配置分开管理

## 2. 推荐目录结构

```text
gaokao-miniapp/
  README.md
  PRODUCT_V1.md
  PROJECT_STRUCTURE.md
  project.config.json
  package.json
  .gitignore

  docs/
  miniprogram/
  cloudfunctions/
  scripts/
  data/
  config/
  logs/
  tests/
```

## 3. 各目录职责

### 根目录关键文件

#### `project.config.json`

这是微信开发者工具识别项目的关键配置文件。

建议至少声明：

- `miniprogramRoot`
- `cloudfunctionRoot`
- `appid`
- `compileType`

没有这个文件，项目导入微信开发者工具时容易出现识别异常。

#### `package.json`

这是项目级 Node 配置文件。

用途包括：

- 管理通用开发脚本
- 管理部分云函数或本地工具依赖
- 统一项目命令入口

#### `.gitignore`

这是必须有的防雷文件。

用于避免以下内容误提交到 Git：

- 原始抓取数据
- 中间清洗结果
- 导出大文件
- 本地日志
- 环境配置
- 依赖目录

### `docs/`

放产品、数据、规则、接口等说明文档。

建议后续放：

- 产品补充文档
- 字段说明
- 接口文档
- 数据更新说明
- 规则说明

### `miniprogram/`

放微信小程序前端代码。

后续内容包括：

- 页面
- 组件
- 样式
- 前端工具函数
- 前端配置

这个目录只负责：

- 用户输入
- 页面展示
- 调用后端接口
- 本地交互逻辑

不负责：

- 抓数据
- 更新数据库
- 保存密钥
- 执行复杂推荐算法

### `cloudfunctions/`

放云函数或轻后端能力。

后续可以用于：

- 学校搜索接口
- 推荐结果接口
- 风险判断接口
- 收藏与备注接口
- 更新任务入口

这个目录本质上是“小程序后端”的第一层。

### `scripts/`

放数据处理脚本。

后续可以包括：

- 北京公办高中标准库生成脚本
- aliases 自动生成脚本
- 数据清洗脚本
- 推荐数据导出脚本
- 官方数据抓取脚本
- 差异比对脚本

这个目录非常关键，它是后续“AI辅助自动更新”的主要落点。

当前建议主技术栈优先使用 Python。

原因：

- 当前仓库已有 Python 数据脚本基础
- Python 更适合抓取、解析、清洗和导出结构化数据
- 后续可自然衔接 pandas、requests、BeautifulSoup 等生态

因此建议在 `scripts/` 下维护：

- `requirements.txt`
- 脚本说明文档

### `data/`

放项目数据文件，但要继续分层。

建议后续再细分：

```text
data/
  raw/
  staging/
  processed/
  exports/
  reference/
```

说明：

- `raw/`：原始抓取数据
- `staging/`：清洗中的中间结果
- `processed/`：正式可用结构化数据
- `exports/`：给小程序或后端使用的导出文件
- `reference/`：手工维护的小型基础表，例如高中梯队配置

版本控制规则必须明确：

- `reference/` 可以进入 Git
- `raw/`、`staging/`、`processed/`、`exports/` 默认不进入 Git
- 这些目录中的内容主要用于本地处理、校验和导出，不作为代码仓库资产

### `config/`

放配置文件，不放业务文案。

后续可以包括：

- 环境配置示例
- 字段配置
- 梯队配置
- 风险标签配置
- 更新任务配置

后续建议按安全边界拆成：

```text
config/
  client/
  server/
```

说明：

- `client/`：前端可见的安全配置，例如展示文案、前端枚举值
- `server/`：仅服务端可见的配置，例如环境变量示例、服务端任务配置

前端绝对不能直接引入 `server/` 级配置。

### `logs/`

放本地运行日志、更新日志、校验报告。

后续建议放：

- 数据更新日志
- 差异检测结果
- 异常报告
- 人工复核记录

版本控制规则：

- `logs/` 默认不进入 Git
- 如需保留目录结构，仅保留占位文件

### `tests/`

放测试脚本。

第一版即使不做复杂自动化测试，也建议至少预留位置给：

- 数据结构校验测试
- 别名匹配测试
- 推荐规则测试
- 风险规则测试

## 4. 为什么要这样分

### 原因 1：前端和数据更新是两套节奏

小程序页面会频繁改交互和样式。
数据脚本会频繁改规则和结构。

如果它们混在一起，后面很难维护。

### 原因 2：小程序不是数据库更新工具

数据库更新、数据抓取、AI辅助解析，这些都应该放在 `scripts/` 和 `cloudfunctions/` 一侧，而不是写在小程序页面里。

### 原因 3：后续自动化要有挂载点

以后如果要做：

- 定时更新
- AI辅助清洗
- 搜索日志回流
- 别名自动补全

都必须有清晰的目录落点。

## 5. 当前阶段建议立刻创建的目录

第一批先建这些就够了：

```text
docs/
miniprogram/
cloudfunctions/
scripts/
data/
config/
logs/
tests/
```

同时建议补齐这几类工程文件：

- `project.config.json`
- `package.json`
- `.gitignore`
- `scripts/requirements.txt`

## 6. 当前阶段建议立刻迁移的文档

建议把当前两份核心文档后续统一放到 `docs/` 下管理：

- 产品文档
- 项目结构文档

当前可以先保留在根目录，等后续文件多起来再统一移动。

## 7. 接下来最合理的开发顺序

目录结构定完后，建议下一步按这个顺序推进：

1. 创建目录骨架
2. 创建北京公办高中标准库模板
3. 创建 aliases 自动生成规则说明
4. 创建推荐数据输出模板
5. 再开始搭小程序前端骨架

## 8. 一句话理解这套结构

可以把这套目录理解成：

- `miniprogram/` 是门店前台
- `cloudfunctions/` 是柜台后的服务台
- `scripts/` 是仓库和加工厂
- `data/` 是库存区
- `docs/` 是操作手册
- `logs/` 是巡检记录

这样后面不管是你自己看，还是我继续帮你做，都不会乱。
