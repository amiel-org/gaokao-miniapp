const verifiedMajorDetails=require("./college-major-details.js");
const FALLBACK_BY_REQUIREMENT={unlimited:["经济管理","法学","外语","新闻传播","公共管理"],physicsChemistry:["计算机类","电子信息类","自动化类","机械/材料/化工","生物医药"],physics:["电子信息","管理科学","工程管理","金融工程","信息管理"],politics:["法学","思想政治教育","国际政治","公共管理","新闻传播"],history:["历史学","考古学","汉语言文学","文化产业管理","文博方向"],geography:["地理科学","地理信息科学","城乡规划","旅游管理","资源环境"],biology:["生物科学","生物技术","食品科学","生态学","生物医药"],chemistry:["化学","应用化学","材料化学","药学","化工方向"],general:["优势专业组","特色实验班","就业优势方向","升学深造方向","交叉复合方向"]};
function normalizeMajorName(value){return String(value||"").replace(/[“”"'\\]/g,"").replace(/\s+/g,"").trim();}
function isReadableMajorName(value){const name=normalizeMajorName(value);if(!name||name.length<2)return false;if(/^[A-Za-z0-9（）()]+$/.test(name))return false;if(/[A-Z]{3,}/.test(name))return false;if(/^[0-9]+$/.test(name))return false;return /[\u4e00-\u9fa5]/.test(name);}
function dedupe(list){const seen={};const result=[];(list||[]).forEach(item=>{const name=normalizeMajorName(item);if(!name||seen[name])return;seen[name]=true;result.push(name);});return result;}
function getVerifiedDetail(group){if(!group)return null;const code=String(group.collegeCode||"");const groupCode=String(group.groupCode||"");const key=code+"_"+groupCode;return (verifiedMajorDetails||[]).find(item=>String(item.id||"").endsWith("_"+key)||(String(item.collegeCode||"")===code&&String(item.groupCode||"")===groupCode))||null;}
function fallbackKey(group){const requirement=(group&&group.subjectRequirement)||{};const raw=requirement.raw||"";const subjects=requirement.subjects||[];if(requirement.mode==="unlimited"||raw.indexOf("不限")>=0||subjects.length===0)return "unlimited";if(subjects.indexOf("物理")>=0&&subjects.indexOf("化学")>=0)return "physicsChemistry";if(subjects.indexOf("物理")>=0)return "physics";if(subjects.indexOf("思想政治")>=0)return "politics";if(subjects.indexOf("历史")>=0)return "history";if(subjects.indexOf("地理")>=0)return "geography";if(subjects.indexOf("生物")>=0)return "biology";if(subjects.indexOf("化学")>=0)return "chemistry";return "general";}
function getFallbackMajors(group){return FALLBACK_BY_REQUIREMENT[fallbackKey(group)]||FALLBACK_BY_REQUIREMENT.general;}
function getFirstlookMajors(group,limit){const detail=getVerifiedDetail(group);const verified=detail&&detail.majors?detail.majors.map(major=>major.majorName).filter(isReadableMajorName):[];const fallback=getFallbackMajors(group);const majors=dedupe([].concat(verified,fallback)).slice(0,limit||5);if(detail&&verified.length){return {majors,sourceLabel:"北京教育考试院 2026 已核验招生专业",hasVerified2026Catalog:true,verifiedDetail:detail};}return {majors,sourceLabel:"按选科生成的初筛专业方向",hasVerified2026Catalog:false,verifiedDetail:null};}
module.exports={getFirstlookMajors};
